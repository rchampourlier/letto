<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Letto | Boards</title>
  </head>
  <body>
    <h1>Boards</h1>
    <ul>
      <% @organizations.each do |organization| %>
        <li>
          <b><%= organization[:display_name] %></b>
          <table>
            <% @boards.each do |board| %>
              <% if board[:organization_id] == organization[:id] %>
                <tr>
                  <td><%= board[:name] %> (<%= board[:id] %>)</td>
                  <td><a href="/boards/<%= board[:id] %>/create_webhook">Create webhook</a>
                </tr>
              <% end %>
            <% end %>
          </table>
        </li>
      <% end %>
    </ul>
  </body>
</html>
