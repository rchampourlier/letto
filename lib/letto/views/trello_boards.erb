<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Letto | Boards</title>
  </head>
  <body>
    <i><a href="/">Back to Home Page</a></i>
    <h1>Boards</h1>
    <ul>
      <% @organizations.each do |organization| %>
        <li>
          <b><%= organization[:display_name] %></b>
          <table>
            <% @boards.zip(@create_webhook_urls).each do |board, url| %>
              <% if board[:organization_id] == organization[:id] %>
                <tr>
                  <td><%= board[:name] %> (<%= board[:id] %>)</td>
                  <td><a href="<%= url %>">Create webhook</a>
                </tr>
              <% end %>
            <% end %>
          </table>
        </li>
      <% end %>
    </ul>
  </body>
</html>
