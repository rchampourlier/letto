<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Letto | Webhooks</title>
  </head>
  <body>
    <h1>Webhooks</h1>
    <% if @webhooks.any? %>
      <ul>
        <% @webhooks.zip(@delete_webhook_urls).each do |webhook, url| %>
          <li><%= webhook[:description] %> <a href="<%= url %>">Delete webhook</a></li>
        <% end %>
      </ul>
    <% else %>
      <p>No webhooks configured yet</p>
    <% end %>
  </body>
</html>
